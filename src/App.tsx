import {Text, View} from "react-native";

export default function App(){
  return(
  <View style={{
      minHeight: "100%",
      justifyContent: "center",
      alignItems: "center"
    }}>
      <Text>Leads1LLC</Text>
  </View>
  );
}
